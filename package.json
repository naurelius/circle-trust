{
  "name": "circle-trust",
  "version": "0.0.1",
  "description": "",
  "keywords": "",
  "license": "",
  "author": "",
  "scripts": {
    "build": "parcel build index.html",
    "dev": "parcel index.html --open",
    "start": "npm run build && npm run dev",
    "build-css": "npx tailwind build style.css -o css/style.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {},
  "dependencies": {
    "autoprefixer": "^10.2.5",
    "bootstrap": "^5.0.0-beta3",
    "eruda-dom": "^2.0.0",
    "postcss": "^8.2.15",
    "tailwindcss": "^2.1.2"
  }
}
